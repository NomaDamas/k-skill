---
"public-restroom-nearby": minor
"toss-securities": minor
---

toss-securities: detect session-expired cases more clearly (including empty-array + invalid auth doctor), add `checkSession()`, and improve quote 403 upstream hinting.

public-restroom-nearby: add optional Kakao Local supplemental source merging (keyword + OL7 category), compute distance locally for all sources, and merge/dedupe into the nearest list.
